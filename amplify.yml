version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - aws amplify update-app --app-id $AWS_APP_ID --custom-rules file://amplify/redirects.json --custom-headers file://amplify/customHttp.yml
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*
